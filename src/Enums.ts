//An enum (short for "enumeration") is a special TypeScript feature that allows you to define a set of named constants. Enums make your code more readable and reduce the use of magic numbers or strings.
const enum Status{
    success=200
}